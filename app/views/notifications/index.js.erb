$('.notification-list').append('<%= j render partial: "notifications/notification_show_all" %>')
<% if @notifications.current_page == @notifications.total_pages %>
  $('#view-more').remove();
<% else %>
  $('#view-more a').attr('href', '<%= url_for(page: @notifications.next_page) %>');
<% end %>
